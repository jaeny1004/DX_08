fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9
# python-jose[cryptography] 에서 옮겨왔다. HS256 하나만 쓰는데도 cryptography
# 패키지 전체(+ecdsa/rsa/pyasn1)를 끌고 와 Vercel 225MB 한도를 넘겼다.
# PyJWT 는 HS256 에 cryptography 를 요구하지 않는다. app/core/security.py 참고.
PyJWT>=2.9
pwdlib[argon2]>=0.2
email-validator>=2.2
# pymupdf(설치 시 53MB)는 배포 의존성에서 뺐다 — Vercel 함수 225MB 한도 초과 때문.
# fitz 는 app/core/parsers.py 에서만 쓰이고 그 파일은 ingest.py 만 import 한다.
# ingest.py 는 .vercelignore 대상이라 배포본에 없고 data/docs/ 도 제외되므로
# 프로덕션에는 파싱할 PDF 자체가 없다. 로컬 문서 재수집 시에만 필요하다:
#   pip install -r requirements-ingest.txt
Pillow>=10.0
supabase>=2.4
openai>=1.40
python-dotenv>=1.0
pydantic>=2.7
httpx>=0.27
bcrypt>=4.1
openpyxl>=3.1
python-docx>=1.1
reportlab>=4.2
# pyproj(24MB) + shapely 와 딸려오던 numpy(51MB)를 뺐다 — Vercel 225MB 한도.
# 유일한 사용처였던 field_survey_report_generator.py 가 쓰던 기능은
# EPSG:5186->4326 변환과 좌표 그릇(.x/.y/.centroid)뿐이라
# app/core/coords.py 의 순수 파이썬 구현으로 대체했다.
# 기하 연산(intersects/buffer/contains 등)이 필요해지면 shapely를 다시 넣을 것.